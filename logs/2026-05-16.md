# 2026-05-16

## 131. Palindrome Partitioning

- Track: LeetCode Top 100 Liked
- Difficulty: Medium
- Topic: String, Dynamic Programming, Backtracking
- Status: Locally implemented and tested; not submitted on LeetCode in this run
- Submission: Not available
- Note: Computer Use access to Google Chrome was denied in this session, so only local verification is recorded.

## 思路

先用 `is_palindrome[i][j]` 预处理每个子串是否为回文，再用 DFS 枚举所有合法切分。

回溯到位置 `start` 时，尝试所有结尾 `end`：

- 如果 `s[start:end+1]` 是回文，就加入当前路径。
- 递归处理后缀。
- 返回时撤销这段子串。

这样可以避免在回溯过程中反复线性判断同一段子串是不是回文。
