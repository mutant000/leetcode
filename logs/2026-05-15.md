# 2026-05-15

## 217. Contains Duplicate

- Track: LeetCode Top 100 Liked
- Difficulty: Easy
- Topic: Hash Table
- Status: Implemented, local smoke tests passed (submission not run in this environment)
- Note: Unable to run remote submission due to no network connectivity to github/leetcode host in this environment.

## 思路

用哈希集合记录已见数字。

按元素遍历数组，若当前值已在集合中则发现重复并返回 `True`；否则加入集合。
遍历结束说明全部不重复，返回 `False`。

## 242. Valid Anagram

- Track: LeetCode Top 100 Liked
- Difficulty: Easy
- Topic: Hash Table
- Status: Implemented, local smoke tests passed (submission not run in this environment)
- Note: Unable to run remote submission due to no network connectivity to github/leetcode host in this environment.

## 思路

先统计 `s` 中每个字符出现次数，再遍历 `t` 做抵消。

若 `t` 中出现了 `s` 没有的字符，或某字符出现次数超出 `s`，可提前返回 `False`。
最终所有计数都回到 0，表示两字符串字符全集与频率完全一致，返回 `True`。

## 88. Merge Sorted Array

- Track: LeetCode Top 100 Liked
- Difficulty: Easy
- Topic: Array, Two Pointers
- Status: Implemented, local smoke tests passed (submission not run in this environment)
- Note: Unable to run remote submission due to no network connectivity to github/leetcode host in this environment.

## 思路

从后往前用 `nums1`、`nums2` 的最后有效下标比较，较大值写入 `nums1` 的尾部。

当某一边取尽后，只需把 `nums2` 的剩余前缀补齐到前面；`nums1` 的剩余部分天然有序可保留。

