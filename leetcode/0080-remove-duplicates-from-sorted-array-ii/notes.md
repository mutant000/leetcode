# 80. Remove Duplicates from Sorted Array II

## 思路

数组已经有序，所以相同数字会连续出现。用 `write` 表示下一个保留元素要写入的位置。

每个数字最多保留两次：如果 `write < 2`，直接保留；否则只要当前数字不等于 `nums[write - 2]`，说明它不会成为第三个重复值，可以写入。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
