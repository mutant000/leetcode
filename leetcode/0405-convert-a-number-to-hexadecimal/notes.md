# 405. Convert a Number to Hexadecimal

## 思路

十六进制的一位对应二进制的 4 位。每次用 `num & 15` 取得最低 4 位对应的十六进制字符，再将 `num` 右移 4 位。

Python 的负整数没有固定宽度，因此先执行 `num & 0xFFFFFFFF`，把输入限制为题目要求的 32 位补码表示。收集到的字符顺序是从低位到高位，最后反转即可。

## 复杂度

- 时间复杂度：`O(1)`，最多处理 8 个十六进制位
- 空间复杂度：`O(1)`
