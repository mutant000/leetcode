# 448. Find All Numbers Disappeared in an Array

## 思路

数组值都在 `[1, n]`，把值 `value` 映射到下标 `value - 1`。

遍历数组时，将每个出现过的值所对应的位置改成负数。遍历结束后，仍然为正数的位置从未被标记，因此这个下标加 `1` 就是缺失的数字。

使用 `abs` 可以避免重复数字把已经标记的负数改回正数。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`，不计算返回结果
