# 222. Count Complete Tree Nodes

## 思路

完全二叉树的特点是：除了最后一层，其他层都是满的，最后一层从左到右填充。

对一个子树分别计算最左路径高度和最右路径高度。如果两个高度相同，说明这个子树是满二叉树，可以直接用 `2^height - 1` 得到节点数。否则继续递归统计左右子树。

这样可以跳过很多满子树，不需要每个节点都遍历一遍。

## 复杂度

- 时间复杂度：`O(log^2 n)`
- 空间复杂度：`O(log n)`，递归栈深度等于树高
