# 5. Longest Palindromic Substring

## 思路

回文串由中心向两边对称扩展。枚举每个位置作为中心，并同时处理奇数长度和偶数长度两种情况。

扩展时只要左右字符相同就继续向外走，停止后返回这次能得到的最大回文区间。遍历所有中心并维护最长区间，最后切片返回答案。

## 复杂度

- Time: `O(n^2)`
- Space: `O(1)`

## 边界

- 单字符字符串本身就是答案。
- 偶数长度回文，例如 `"bb"`。
- 多个最长答案同时存在时，返回任意一个都合法。
