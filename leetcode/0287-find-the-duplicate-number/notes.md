# 287. Find the Duplicate Number

## 思路

把数组看成链表：下标是节点，`nums[index]` 是下一跳。因为数字范围是 `1..n` 且有重复值，所以一定会形成环，重复数字就是环入口。

先用快慢指针找到环内相遇点，再让一个指针回到起点，两个指针同步前进，下一次相遇的位置就是重复数字。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
