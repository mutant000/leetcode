# 401. Binary Watch

## 思路

合法时间只有 `12 * 60 = 720` 种，可以直接枚举所有小时和分钟。

一个整数二进制表示中的 `1` 的数量就是对应区域点亮的 LED 数量。若 `hour.bit_count() + minute.bit_count()` 等于 `turnedOn`，就把时间加入答案，并把分钟格式化成两位。

## 复杂度

- 时间复杂度：`O(12 * 60)`，可视为 `O(1)`
- 空间复杂度：`O(1)`，不计算返回结果
