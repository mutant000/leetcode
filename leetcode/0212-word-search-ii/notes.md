# 212. Word Search II

## 思路

把所有目标单词放进 Trie，然后从棋盘每个格子开始 DFS。

DFS 时沿 Trie 匹配当前路径，如果当前字符不在 Trie 子节点中，说明这条路径不可能形成目标单词，立刻剪枝。

当走到带有完整单词的 Trie 节点时，把单词加入答案，并把该节点的 `word` 清空，避免重复加入。回溯后如果某个 Trie 分支已经没有用，也可以删除它继续剪枝。

## 复杂度

- 时间复杂度：最坏 `O(mn * 4^l)`，实际会被 Trie 大量剪枝
- 空间复杂度：`O(total characters)`
