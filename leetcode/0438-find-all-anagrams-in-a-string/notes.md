# 438. Find All Anagrams in a String

## 思路

固定长度滑动窗口。窗口长度始终等于 `len(p)`，用计数器记录窗口内字符频率，并与 `p` 的字符频率比较。

每次窗口右移时，删除离开的字符、加入新字符；计数降为 `0` 的字符从计数器中删掉，便于直接比较两个计数器。

## 复杂度

- Time: `O(n * alphabet)`，这里小写字母集合固定，可视作 `O(n)`
- Space: `O(alphabet)`

## 边界

- `p` 比 `s` 长。
- 多个连续窗口都是异位词。
- 字符计数归零后需要清理。
