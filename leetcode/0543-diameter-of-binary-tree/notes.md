# 543. Diameter of Binary Tree

## 思路

二叉树直径是任意两个节点之间最长路径的边数。

对每个节点来说，如果最长路径经过它，那么长度就是：

```text
左子树深度 + 右子树深度
```

所以递归计算深度时，顺便更新全局最大直径。

## 复杂度

- Time: `O(n)`
- Space: `O(h)`

## 边界

- 空树直径是 `0`。
- 只有一个节点时，没有边，直径也是 `0`。
