# 436. Find Right Interval

## 思路

先保存每个区间的 `(起点, 原下标)`，并按照起点排序。

对于每个区间的终点 `end`，使用 `bisect_left` 二分查找第一个大于等于 `end` 的起点：

- 找到时，返回这个起点对应的原下标。
- 找不到时，返回 `-1`。

题目保证所有起点不同，因此符合条件的最小起点对应唯一答案。

## 复杂度

- 时间复杂度：`O(n log n)`
- 空间复杂度：`O(n)`
