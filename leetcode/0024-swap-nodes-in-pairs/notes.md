# 24. Swap Nodes in Pairs

## 思路

用虚拟头节点统一处理头部交换。`previous` 指向当前要交换的一对节点前面的位置，取出 `first` 和 `second` 后调整三条指针。

交换完成后，`first` 已经变成这一对的尾部，下一轮从它后面继续。

## 复杂度

- Time: `O(n)`
- Space: `O(1)`

## 边界

- 空链表。
- 只有一个节点。
- 节点数为奇数时，最后一个节点保持原位。
