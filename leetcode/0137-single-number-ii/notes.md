# 137. Single Number II

## 思路

除了一个数只出现一次，其他数都出现三次。逐位统计所有数字在每个二进制位上的 `1` 的数量。

如果某一位的 `1` 的数量对 `3` 取余后仍然是 `1`，说明只出现一次的数字在这一位是 `1`。最后把这些位拼回答案。

Python 的负数是无限符号位，所以按 32 位整数处理；如果结果超过有符号整数正数范围，就转换回负数。

## 复杂度

- 时间复杂度：`O(32n)`，可以看作 `O(n)`
- 空间复杂度：`O(1)`
