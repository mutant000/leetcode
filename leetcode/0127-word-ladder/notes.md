# 127. Word Ladder

## 思路

每次只能改变一个字符，可以把单词之间的转换看成图的边。

为了快速找到只差一个字符的单词，把每个单词变成通配模式，例如 `hot` 可以生成 `*ot`、`h*t`、`ho*`。拥有同一个模式的单词彼此可以一步转换。

从 `beginWord` 开始 BFS，第一次到达 `endWord` 时的层数就是最短转换序列长度。

## 复杂度

- 时间复杂度：`O(n * l^2)`，`n` 是单词数，`l` 是单词长度
- 空间复杂度：`O(n * l)`
