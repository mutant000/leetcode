# 130. Surrounded Regions

## 思路

不会被包围的 `O` 一定和边界上的 `O` 连通。

所以先从四条边界出发，把所有能连到边界的 `O` 标记成临时字符 `S`。这些位置不能被翻转。

最后再次遍历棋盘：剩下的 `O` 都是被包围的，改成 `X`；临时标记 `S` 改回 `O`。

## 复杂度

- 时间复杂度：`O(mn)`
- 空间复杂度：`O(mn)`，队列最多保存整个棋盘的格子
