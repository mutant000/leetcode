# 373. Find K Pairs with Smallest Sums

## 思路

两个数组都是升序。对于固定的 `nums1[i]`，它和 `nums2[0]`、`nums2[1]`、... 组成的配对和也是升序。

先把每个 `nums1[i]` 与 `nums2[0]` 的配对放入最小堆。每次弹出当前最小配对后，再把同一个 `nums1[i]` 与 `nums2` 下一个元素组成的新配对放入堆。

这样堆里始终保存每条升序配对链的下一个候选。

## 复杂度

- 时间复杂度：`O(k log min(k, n))`
- 空间复杂度：`O(min(k, n))`
