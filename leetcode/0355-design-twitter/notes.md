# 355. Design Twitter

## 思路

每个用户保存自己的推文列表，每条推文带递增时间戳。

取动态时，把自己和关注者的最新推文放进堆，每次弹出最新的一条，再把同一用户的下一条旧推文放回堆，直到拿到 10 条。

## 复杂度

- `postTweet`：`O(1)`
- `follow` / `unfollow`：`O(1)`
- `getNewsFeed`：`O(f log f)`，`f` 是关注用户数量加自己
