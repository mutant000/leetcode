# 215. Kth Largest Element in an Array

## 思路

第 `k` 大等价于降序下标 `k - 1`。用快速选择，每次三路分区后只保留目标下标所在的一侧继续处理。

三路分区把大于、等于、小于基准值的元素分成三段，重复值很多时也能一次跳过整段等于基准值的区间。

## 复杂度

- Time: average `O(n)`, worst `O(n^2)`
- Space: `O(1)`

## 边界

- 有重复值。
- `k = 1` 或 `k = len(nums)`。
- 数组已经近似有序。
