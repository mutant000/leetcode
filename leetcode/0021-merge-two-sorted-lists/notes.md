# 21. Merge Two Sorted Lists

## 思路

两个链表本来都是升序，所以每次比较两个链表当前节点，把较小的节点接到新链表后面。

这里用 `dummy` 虚拟头节点简化操作：

1. `current` 指向新链表最后一个节点。
2. 每次选择 `list1` 和 `list2` 中更小的节点接上。
3. 某个链表走完后，把另一个链表剩余部分整体接上。

## 复杂度

- Time: `O(m + n)`
- Space: `O(1)`

## 边界

- 某个链表一开始就是空。
- 两个节点值相等时，接任意一个都可以。
