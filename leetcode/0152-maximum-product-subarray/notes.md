# 152. Maximum Product Subarray

## 思路

乘积和加法不同，负数可能把最小乘积变成最大乘积。

遍历数组时同时维护以当前位置结尾的最大乘积和最小乘积。遇到负数时，最大和最小会互换角色，然后用当前数字本身或接上之前乘积来更新状态。

每一步用当前最大乘积更新全局答案。

## 复杂度

- Time: `O(n)`
- Space: `O(1)`

## 边界

- 数组只有一个元素。
- 出现 `0` 时，子数组可以从之后重新开始。
- 负数数量为偶数或奇数时，最优区间可能不同。
