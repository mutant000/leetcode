# 380. Insert Delete GetRandom O(1)

## 思路

用数组保存所有值，保证 `getRandom` 可以等概率随机取一个下标；用哈希表记录每个值在数组里的下标。

删除时把要删的位置和数组最后一个值交换，再弹出最后一个元素。这样不需要移动一整段数组，插入、删除、随机获取平均都是 `O(1)`。

## 复杂度

- `insert` / `remove` / `getRandom` 平均时间复杂度：`O(1)`
- 空间复杂度：`O(n)`
