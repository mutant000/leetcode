# 443. String Compression

## 思路

使用两个指针原地压缩：

- `read` 寻找一组连续相同字符的结束位置。
- `write` 指向压缩结果下一次写入的位置。

每组先写入字符。如果出现次数大于 `1`，再把次数转换成字符串并逐位写入。例如出现 `12` 次时，需要写入字符 `"1"` 和 `"2"`。

最后 `write` 就是压缩后的长度，答案位于 `chars[:write]`。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
