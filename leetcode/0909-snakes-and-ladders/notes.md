# 909. Snakes and Ladders

## 思路

每个格子可以看成图里的一个点。一次掷骰子能从当前格子走到后面 `1` 到 `6` 个格子，如果落点有蛇或梯子，就必须跳到对应目标格。

题目要求最少移动次数，所以用 BFS。队列里保存当前格子和已经走的步数，第一次到达终点时就是最少次数。

棋盘编号是从左下角开始，并且每一行方向交替，所以需要一个辅助函数把格子编号转换成 `board` 的行列坐标。

## 复杂度

- 时间复杂度：`O(n^2)`，每个格子最多入队一次
- 空间复杂度：`O(n^2)`
