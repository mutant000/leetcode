# 76. Minimum Window Substring

## 思路

用滑动窗口维护当前区间内是否已经覆盖 `t` 的全部字符。

`need` 记录窗口还欠每个字符多少个，`missing` 记录总共还缺多少个字符。右指针扩张窗口，等所有字符都满足后，左指针不断右移缩小窗口，并在每次满足条件时更新最短答案。

## 复杂度

- 时间复杂度：`O(m + n)`
- 空间复杂度：`O(k)`，`k` 是字符种类数
