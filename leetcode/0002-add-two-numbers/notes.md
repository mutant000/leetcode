# 2. Add Two Numbers

## 思路

链表低位在前，正好可以从头开始模拟竖式加法。每次取两个当前节点的值和进位，生成当前位节点，再把进位传到下一轮。

循环条件包含 `carry`，所以最后多出来的一位也会被补上。

## 复杂度

- Time: `O(max(m, n))`
- Space: `O(max(m, n))`

## 边界

- 两个链表长度不同。
- 最后一位相加后还有进位。
- 输入中有一个链表提前为空。
