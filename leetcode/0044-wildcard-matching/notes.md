# Wildcard Matching

## 思路

用双指针贪心匹配。

- 普通字符或 `?` 能匹配时，两个指针一起右移。
- 遇到 `*` 时，先记录这个 `*` 的位置，并暂时让它匹配空串。
- 后面如果匹配失败，就回到上一个 `*`，让它多匹配一个字符。

最后如果模式串剩下的都是 `*`，它们可以匹配空串。

## 复杂度

- 时间复杂度：`O(m + n)`
- 空间复杂度：`O(1)`
