# 433. Minimum Genetic Mutation

## 思路

每个合法基因字符串可以看成图里的点。如果两个字符串只差一个字符，就可以一步突变到达。

从 `startGene` 开始 BFS，每次尝试把每个位置替换成 `A/C/G/T`，只有生成的新字符串在 `bank` 里并且没访问过，才加入队列。

BFS 第一次遇到 `endGene` 时，步数就是最少突变次数。如果无法到达，返回 `-1`。

## 复杂度

- 时间复杂度：`O(b * l * 4)`，`b` 是 bank 数量，`l` 是基因长度
- 空间复杂度：`O(b)`
