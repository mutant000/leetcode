# 400. Nth Digit

## 思路

无限整数序列可以按数字位数分组：

- 1 位数有 9 个，共贡献 `9 * 1` 位
- 2 位数有 90 个，共贡献 `90 * 2` 位
- 3 位数有 900 个，共贡献 `900 * 3` 位

先从 `n` 中减去完整分组的长度，直到找到第 `n` 位所在的位数分组。随后用除法定位具体数字，用取模定位该数字中的具体一位。

## 复杂度

- 时间复杂度：`O(log n)`
- 空间复杂度：`O(1)`
