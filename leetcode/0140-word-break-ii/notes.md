# Word Break II

## 思路

用记忆化 DFS 返回从某个下标开始能组成的所有句子。

对当前位置 `index`，枚举下一个单词的结束位置。如果前缀在词典中，就递归求后缀句子，再把当前单词接到前面。

`lru_cache` 可以避免同一个下标被重复求解。用词典中最长单词长度限制枚举范围，减少无效尝试。

## 复杂度

- 时间复杂度：和输出规模相关，最坏可能指数级
- 空间复杂度：和缓存及输出规模相关
