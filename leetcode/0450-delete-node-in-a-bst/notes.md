# 450. Delete Node in a BST

## 思路

利用 BST 的大小关系迭代查找目标节点，同时记录父节点。找到后分三种情况：

- 没有左子树：直接返回右子树。
- 没有右子树：直接返回左子树。
- 左右子树都存在：找到右子树中的最小节点，也就是中序后继。用后继值替换当前值，再删除这个至多只有右孩子的后继节点。

最后让父节点直接连接被删除节点的唯一孩子。如果删除的是根节点，就返回它的孩子作为新根。

## 复杂度

- 时间复杂度：`O(h)`
- 空间复杂度：`O(1)`
