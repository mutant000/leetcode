# 94. Binary Tree Inorder Traversal

## 思路

中序遍历顺序是：

```text
左子树 -> 当前节点 -> 右子树
```

用 `stack` 模拟递归：

1. 当前节点不为空时，一直往左走，并把经过的节点放入栈。
2. 走到空后，从栈里弹出一个节点并输出它的值。
3. 再去这个节点的右子树。

## 复杂度

- Time: `O(n)`
- Space: `O(n)`
