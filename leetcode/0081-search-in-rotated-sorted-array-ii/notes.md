# Search in Rotated Sorted Array II

## 思路

在旋转有序数组中二分查找。

和无重复版本不同，如果 `nums[left] == nums[mid] == nums[right]`，无法判断哪一边有序，只能同时收缩两端。

否则至少有一边是有序的：

- 左半边有序，就判断 `target` 是否落在左半边。
- 右半边有序，就判断 `target` 是否落在右半边。

## 复杂度

- 平均时间复杂度：`O(log n)`
- 最坏时间复杂度：`O(n)`，大量重复元素时可能退化
- 空间复杂度：`O(1)`
