# 449. Serialize and Deserialize BST

## 思路

序列化时使用栈记录 BST 的前序遍历，不需要保存空节点。

反序列化时同样使用栈：

- 新值小于栈顶时，它是栈顶节点的左孩子。
- 新值大于栈顶时，不断弹出更小的祖先。最后弹出的节点就是新值的父节点，新值是它的右孩子。

BST 的大小关系使前序序列能够唯一恢复原树。迭代写法也能避免极端单链树超过 Python 的递归深度。

## 复杂度

- 序列化时间复杂度：`O(n)`
- 反序列化时间复杂度：`O(n)`
- 空间复杂度：`O(n)`，用于序列和栈
