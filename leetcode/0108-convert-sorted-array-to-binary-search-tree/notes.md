# 108. Convert Sorted Array to Binary Search Tree

## 思路

数组已经升序，要构造高度平衡 BST，最自然的方法是每次选择中间元素做根节点。

1. 中间元素作为当前子树根节点。
2. 左半边递归构造左子树。
3. 右半边递归构造右子树。

因为每次都从中间切开，所以高度会尽量平衡。

## 复杂度

- Time: `O(n)`
- Space: `O(log n)`，递归栈平均高度为树高

## 边界

- 空区间返回 `None`。
- 单个数字直接变成一个节点。
