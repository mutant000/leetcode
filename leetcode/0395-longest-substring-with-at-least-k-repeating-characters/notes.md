# 395. Longest Substring with At Least K Repeating Characters

## 思路

先统计当前字符串中每个字符的次数。出现次数小于 `k` 的字符一定不能出现在任何有效子串中，因此可以用它把字符串分割成更小的问题并递归求最大值。

如果当前字符串里的所有字符都至少出现 `k` 次，整个字符串就是当前最长答案。

## 复杂度

- 时间复杂度：`O(26n)`，可写作 `O(n)`
- 空间复杂度：`O(26)` 递归深度，忽略分割字符串结果
