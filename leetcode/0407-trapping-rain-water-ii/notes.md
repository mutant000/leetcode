# 407. Trapping Rain Water II

## 思路

二维地形能否存水取决于四周最低的有效边界。先把所有边界格子加入最小堆，再不断从当前最低边界向内部扩展，类似从地图外侧逐层灌水。

若相邻格子比当前边界低，两者高度差就是该格子能存的水；随后把 `max(边界高度, 相邻格子高度)` 作为这个方向的新边界放入堆中。访问标记保证每个格子只处理一次。

## 复杂度

- 时间复杂度：`O(mn log(mn))`
- 空间复杂度：`O(mn)`
