# Fraction to Recurring Decimal

## 思路

模拟长除法。

先处理符号和整数部分，然后不断把余数乘以 10 得到下一位小数。

如果某个余数第二次出现，说明小数开始循环。记录每个余数第一次出现时结果字符串的位置，就能在对应位置插入左括号，末尾加右括号。

## 复杂度

- 时间复杂度：`O(k)`，`k` 是小数展开到循环前的长度
- 空间复杂度：`O(k)`
