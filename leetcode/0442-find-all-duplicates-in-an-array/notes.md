# 442. Find All Duplicates in an Array

## 思路

数组中的值都在 `[1, n]`，因此可以把值 `value` 映射到下标 `value - 1`。

第一次遇到一个值时，把对应位置改成负数作为访问标记。再次遇到同一个值时，对应位置已经是负数，说明这个值出现了两次，把它加入答案。

读取当前值时要使用绝对值，因为这个位置本身可能已经被标记。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`，不计算返回结果
