# 101. Symmetric Tree

## 思路

判断一棵树是否对称，本质是判断左子树和右子树是否互为镜像。

镜像关系需要同时满足：

1. 两个节点都为空，说明这一边对称。
2. 只有一个为空，不对称。
3. 两个节点值相同，并且 `左.left` 对应 `右.right`，`左.right` 对应 `右.left`。

## 复杂度

- Time: `O(n)`
- Space: `O(h)`

## 边界

- 空树是对称的。
- 节点值相同但结构不同，也不是对称。
