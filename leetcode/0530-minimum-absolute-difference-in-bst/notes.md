# 530. Minimum Absolute Difference in BST

## 思路

二叉搜索树的中序遍历结果是升序。最小绝对差一定出现在升序序列的相邻两个数之间。

遍历时只需要保存上一个访问过的值 `previous`，每访问一个新节点，就用当前值减去 `previous` 更新答案。

## 复杂度

- 时间复杂度：`O(n)`，每个节点访问一次
- 空间复杂度：`O(h)`，`h` 是树高
