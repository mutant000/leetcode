# 453. Minimum Moves to Equal Array Elements

## 思路

每次给 `n - 1` 个元素加 `1`，相对效果等价于给剩下的一个元素减 `1`。

因此可以把问题转换成：每次将一个元素减 `1`，直到所有元素都等于数组最小值。每个元素需要减少 `value - minimum` 次，将这些差值相加就是最少操作数。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
