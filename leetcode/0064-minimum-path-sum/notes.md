# 64. Minimum Path Sum

## 思路

每个格子的最小路径和只可能来自上方或左侧。

用一维 `dp` 保存当前行各列的最小路径和。第一行只能从左往右累加；从第二行开始，第一列只能从上方下来，其余位置取 `min(上方, 左侧) + 当前格子值`。

## 复杂度

- Time: `O(m * n)`
- Space: `O(n)`

## 边界

- 只有一行时只能一直向右。
- 只有一列时只能一直向下。
- 网格值可以为 `0`，仍然按普通最短路径和处理。
