# 20. Valid Parentheses

## 思路

用栈维护还没有匹配的左括号。

遍历字符串：

1. 遇到左括号时入栈。
2. 遇到右括号时，栈顶必须是对应类型的左括号。
3. 如果栈为空却遇到右括号，或者栈顶类型不匹配，直接返回 `False`。

遍历结束后，只有栈为空才说明所有括号都已经按正确顺序匹配。

## 复杂度

- Time: `O(n)`
- Space: `O(n)`

## 边界

- 第一个字符就是右括号时无效。
- 形如 `([)]` 的数量相等但嵌套顺序错误，必须返回 `False`。
- 遍历结束后栈里还有左括号，说明没有闭合。
