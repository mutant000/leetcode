# Distinct Subsequences

## 思路

用一维动态规划统计方案数。

`dp[j]` 表示用已经遍历过的 `s` 字符，可以组成 `t` 前 `j` 个字符的方案数。

遍历 `s` 的每个字符时，倒序更新 `t`：

- 如果当前字符等于 `t[j]`，它可以接在所有组成 `t` 前 `j` 个字符的方案后面。
- 所以执行 `dp[j + 1] += dp[j]`。

倒序更新是为了避免同一个 `s` 字符被重复使用。

## 复杂度

- 时间复杂度：`O(len(s) * len(t))`
- 空间复杂度：`O(len(t))`
