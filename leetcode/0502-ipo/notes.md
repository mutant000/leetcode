# 502. IPO

## 思路

项目能不能做取决于当前资金 `w` 是否达到资本要求。先按资本要求排序项目。

每一轮把所有当前资金能做的项目放入最大堆，堆里按利润排序。然后选择利润最高的项目，增加资金。

最多做 `k` 个项目；如果堆为空，说明当前资金没有任何可做项目，提前结束。

## 复杂度

- 时间复杂度：`O(n log n + k log n)`
- 空间复杂度：`O(n)`
