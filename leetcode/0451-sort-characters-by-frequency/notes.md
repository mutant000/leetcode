# 451. Sort Characters By Frequency

## 思路

先用 `Counter` 统计每个字符出现的次数，再用 `most_common()` 按频率从高到低取得字符。

对于每个 `(字符, 频率)`，把该字符重复相应次数并连接起来。频率相同的字符可以采用任意顺序。

## 复杂度

- 时间复杂度：`O(n + k log k)`，`k` 是不同字符数量
- 空间复杂度：`O(n)`
