# Scramble String

## 思路

递归判断两个字符串能不能通过切分和交换得到。

每次把字符串切成左右两段，有两种情况：

- 不交换：左对左，右对右。
- 交换：左对右，右对左。

为了减少重复计算，用 `lru_cache` 记住已经判断过的字符串对。判断前先比较排序后的字符，如果字符组成不同，直接返回 `False`。

## 复杂度

- 时间复杂度：约 `O(n^4)`，递归状态和切分点都会产生计算
- 空间复杂度：`O(n^3)`，主要是缓存状态
