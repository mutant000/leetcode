# Dungeon Game

## 思路

从终点反推到起点。

`dp[col]` 表示走到当前位置时，骑士至少需要多少生命值，才能安全到达终点。

当前位置只能往右或往下走，所以先取右边和下边需要生命值的较小值，再减去当前格子的效果：

```text
need = min(right, down) - dungeon[row][col]
```

生命值不能小于 1，所以每格取 `max(1, need)`。

## 复杂度

- 时间复杂度：`O(m * n)`
- 空间复杂度：`O(n)`
