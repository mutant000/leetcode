# Combinations

## 思路

用回溯从 `1..n` 中选择 `k` 个数。

`path` 保存当前组合。如果剩余数字数量不足以填满 `k`，后面的分支不用继续尝试，所以循环上界用：

```text
n - need + 1
```

代码中的 `range` 右端点多加 1。

## 复杂度

- 时间复杂度：`O(k * C(n, k))`
- 空间复杂度：`O(k)`，不计返回结果
