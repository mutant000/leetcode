# Valid Number

## 思路

按字符扫描，用几个状态判断是否合法：

- 是否已经出现数字。
- 是否已经出现小数点。
- 是否已经出现指数 `e/E`。
- 指数后面是否已经出现数字。

符号 `+/-` 只能出现在开头，或者紧跟在 `e/E` 后面；小数点不能出现在指数后面；指数前必须已经有数字，指数后也必须有数字。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
