# 240. Search a 2D Matrix II

## 思路

从右上角开始搜索。当前位置比 `target` 大时，当前列下面的数只会更大，所以左移；当前位置比 `target` 小时，当前行左边的数只会更小，所以下移。

每一步都能排除一整行或一整列。

## 复杂度

- Time: `O(m + n)`
- Space: `O(1)`

## 边界

- 目标在右上角或左下角。
- 目标不存在。
- 单行或单列矩阵。
