# 201. Bitwise AND of Numbers Range

## 思路

区间内所有数字按位与后，只会保留 `left` 和 `right` 的公共二进制前缀。

当 `left` 和 `right` 不相等时，说明后面的低位在区间变化中一定会被清零。不断把两者右移，直到它们相等，再把公共前缀左移回原来的位置。

## 复杂度

- 时间复杂度：`O(log n)`
- 空间复杂度：`O(1)`
