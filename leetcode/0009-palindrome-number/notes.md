# 9. Palindrome Number

## 思路

负数不可能是回文；末尾是 `0` 的正数也不可能是回文，除非它本身就是 `0`。

只反转数字的后一半。循环中每次取出 `x` 的最后一位，加到 `reversed_half`。当原数字剩下的前半部分不大于反转后的后半部分时停止。

偶数位数字需要两半相等；奇数位数字可以去掉中间位后比较。

## 复杂度

- 时间复杂度：`O(log n)`
- 空间复杂度：`O(1)`
