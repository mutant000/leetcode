# Maximal Rectangle

## 思路

把每一行当作柱状图的底部。

遍历矩阵时，用 `heights[col]` 记录当前列往上连续的 `"1"` 高度。每处理完一行，就把这组高度交给“柱状图最大矩形”的单调栈算法。

单调栈中保存高度递增的下标；当当前高度变小时，就可以计算被弹出柱子的最大矩形面积。

## 复杂度

- 时间复杂度：`O(m * n)`
- 空间复杂度：`O(n)`
