# 128. Longest Consecutive Sequence

## 思路

把所有数字放进哈希集合。只有当 `value - 1` 不存在时，`value` 才是一段连续序列的起点，然后从这个起点向右数长度。

这样每个数字最多作为序列成员被访问一次，不会重复从中间位置扩展。

## 复杂度

- Time: `O(n)`
- Space: `O(n)`

## 边界

- 空数组。
- 有重复数字。
- 存在负数或序列不从最小值开始。
