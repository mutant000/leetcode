# 31. Next Permutation

## 思路

从右往左找第一个下降位置 `pivot`，也就是 `nums[pivot] < nums[pivot + 1]`。它右边的后缀已经是降序，说明这段后缀已经是当前能形成的最大排列。

如果找到 `pivot`，再从右往左找第一个比它大的数交换，让整体刚好变大一点。最后把后缀反转成升序，得到最小的更大排列。

如果找不到 `pivot`，整个数组是降序，直接反转为最小排列。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
