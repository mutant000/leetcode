# Permutation Sequence

## 思路

不用生成所有排列，直接用阶乘定位第 `k` 个排列。

长度为 `n` 时，每个首位数字会对应 `(n - 1)!` 个排列。先把 `k` 转成从 0 开始，然后：

1. 用 `k // block_size` 找当前位要选哪个数字。
2. 用 `k % block_size` 进入下一层。
3. 被选过的数字从候选列表中移除。

## 复杂度

- 时间复杂度：`O(n^2)`，列表 `pop(index)` 会移动元素
- 空间复杂度：`O(n)`
