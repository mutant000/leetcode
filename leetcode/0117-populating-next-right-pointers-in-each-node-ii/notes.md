# 117. Populating Next Right Pointers in Each Node II

## 思路

利用上一层已经建立好的 `next` 指针横向遍历当前层，同时用虚拟头节点串起下一层。

每处理一个节点，就把它的左右孩子接到下一层链表尾部。当前层走完后，跳到下一层虚拟头的 `next` 继续处理。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
