# 133. Clone Graph

## 思路

图里可能有环，所以不能简单递归复制，否则会无限循环。

用哈希表 `clones` 记录“原节点 -> 克隆节点”。如果一个节点已经克隆过，就直接返回对应的新节点；否则先创建新节点并放进哈希表，再递归克隆它的邻居。

这样每个原节点只会被复制一次，同时邻接关系也会被完整重建。

## 复杂度

- 时间复杂度：`O(V + E)`
- 空间复杂度：`O(V)`，保存克隆映射和递归栈
