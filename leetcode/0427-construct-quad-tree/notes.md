# 427. Construct Quad Tree

## 思路

先为 `grid` 建立二维前缀和。这样可以在 `O(1)` 时间内得到任意正方形区域中 `1` 的数量。

递归处理一个区域：

- 和为 `0`：区域全是 `0`，创建值为 `False` 的叶子。
- 和等于区域面积：区域全是 `1`，创建值为 `True` 的叶子。
- 否则：把区域平均分成左上、右上、左下、右下四部分，继续递归。

非叶子节点的 `val` 可以是任意值，代码统一使用 `True`。

## 复杂度

- 时间复杂度：`O(n^2)`
- 空间复杂度：`O(n^2)`，包括前缀和与四叉树节点
