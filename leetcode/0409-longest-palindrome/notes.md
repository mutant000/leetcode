# 409. Longest Palindrome

## 思路

回文串中每个字符通常需要成对放在左右两侧，最多只有一个出现奇数次的字符可以放在中心。

遍历字符串，用集合记录尚未配对的字符。再次遇到同一字符时组成一对，答案增加 2，并从集合移除它。遍历结束后，如果集合仍非空，就可以任选一个字符放在回文中心，答案再增加 1。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(k)`，`k` 为不同字符数量
