# 149. Max Points on a Line

## 思路

枚举每个点作为基准点，统计它和后面每个点形成的斜率。相同斜率代表这些点和基准点在同一条直线上。

为了避免浮点误差，用 `(delta_x, delta_y)` 的最简整数比表示斜率，并用 `gcd` 约分。垂直线和水平线单独规范成固定形式。

某个斜率出现 `k` 次，说明加上基准点共有 `k + 1` 个点在同一直线上。

## 复杂度

- 时间复杂度：`O(n^2)`
- 空间复杂度：`O(n)`
