# 98. Validate Binary Search Tree

## 思路

二叉搜索树的中序遍历结果必须严格递增。

用栈模拟中序遍历：
- 先一路向左，把节点压栈。
- 弹出节点时，它就是当前顺序里的下一个值。
- 如果当前值小于等于上一个访问过的值，就不是合法 BST。

注意这里必须是严格递增，相等也不合法。

## 复杂度

- Time: `O(n)`
- Space: `O(h)`

## 边界

- 空树。
- 只有一个节点。
- 左右子树局部合法，但整体范围不合法。
