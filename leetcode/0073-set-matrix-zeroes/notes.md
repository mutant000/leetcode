# 73. Set Matrix Zeroes

## 思路

用第一行和第一列作为标记数组。先单独记录原始第一行、第一列是否需要清零，再用 `matrix[row][0]` 和 `matrix[0][col]` 标记其它位置所在行列是否有零。

第二次扫描内部区域时，根据标记清零。最后处理第一行和第一列。

## 复杂度

- Time: `O(mn)`
- Space: `O(1)`

## 边界

- 零出现在第一行或第一列。
- 矩阵只有一行或一列。
- 多个零共享行列。
