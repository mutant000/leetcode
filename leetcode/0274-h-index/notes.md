# 274. H-Index

## 思路

把引用次数从大到小排序。扫描到第 `index` 篇论文时，如果它的引用数至少是 `index`，说明至少有 `index` 篇论文引用数不小于 `index`。

一直更新满足条件的最大 `index`，遇到不满足时后面的引用数只会更小，可以停止。

## 复杂度

- 时间复杂度：`O(n log n)`
- 空间复杂度：`O(1)`，不计排序内部开销
