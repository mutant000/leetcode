# 142. Linked List Cycle II

## 思路

先用快慢指针判断是否有环。相遇后，把一个指针放回头节点，另一个留在相遇点，两者每次都走一步，再次相遇的位置就是入环点。

原因是头节点到入环点的距离，和相遇点继续走到入环点的距离在环长意义下相等。

## 复杂度

- Time: `O(n)`
- Space: `O(1)`

## 边界

- 无环链表。
- 环从头节点开始。
- 只有一个节点且自环。
