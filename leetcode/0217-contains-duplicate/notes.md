# 217. Contains Duplicate

## 思路

用哈希集合记录已经出现过的数字。

遍历数组：

1. 如果数字已经在集合中，说明出现重复，立即返回 `True`。
2. 否则把数字加入集合继续扫描。

扫描结束都未命中，说明所有元素唯一，返回 `False`。

## 复杂度

- Time: `O(n)`
- Space: `O(n)`

## 边界

- 空数组或只有一个元素时，肯定不存在重复值。
- 包含负数、重复多次、以及长数组都能正常处理。
