# Sudoku Solver

## 思路

用回溯填写空格，同时维护每一行、每一列、每个 `3 x 3` 宫里已经使用的数字。

每次递归时，先从剩余空格中挑可选数字最少的格子，这样能更早剪枝。

尝试一个数字时：

- 写入棋盘。
- 加入行、列、宫的集合。
- 继续递归。
- 如果失败，就撤销这一步。

## 复杂度

- 时间复杂度：最坏接近 `O(9^m)`，`m` 是空格数
- 空间复杂度：`O(m)`
