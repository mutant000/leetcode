# Combination Sum III

## 思路

从 `1` 到 `9` 中选 `k` 个不同数字，使它们的和为 `n`。

用回溯维护当前路径、还需要选几个数、还差多少和。每次从更大的数字继续选，保证不会重复使用数字，也不会出现重复组合。

如果当前数字已经大于剩余目标和，可以提前停止。

## 复杂度

- 时间复杂度：`O(C(9, k) * k)`
- 空间复杂度：`O(k)`，不计答案
