# 199. Binary Tree Right Side View

## 思路

从右侧看一棵树，每一层能看到的就是这一层最右边的节点。

用 BFS 按层遍历：
- 每一层开始时记下队列长度。
- 依次弹出这一层的节点。
- 当处理到这一层最后一个节点时，把它的值加入答案。

## 复杂度

- Time: `O(n)`
- Space: `O(w)`

## 边界

- 空树。
- 只有一个节点。
- 树偏左时也要返回每层唯一能看到的节点。
