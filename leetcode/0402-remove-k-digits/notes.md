# 402. Remove K Digits

## 思路

要让结果尽量小，应该优先删除高位中比后一个数字更大的数字。

从左到右维护一个单调不减栈。当当前数字比栈顶更小时，不断弹出栈顶并消耗删除次数。如果扫描结束后仍有删除次数，数字已经整体单调不减，此时删除末尾的数字最优。

最后去掉前导零；如果结果为空，就返回 `"0"`。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(n)`
