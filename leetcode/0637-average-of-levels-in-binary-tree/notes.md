# 637. Average of Levels in Binary Tree

## 思路

用队列做层序遍历。每次进入循环时，队列里刚好是当前层的所有节点。

记录这一层的节点数量 `level_size`，弹出这些节点并累加它们的值，同时把下一层节点放入队列。当前层遍历完后，用总和除以数量就是这一层的平均值。

## 复杂度

- 时间复杂度：`O(n)`，每个节点访问一次
- 空间复杂度：`O(w)`，`w` 是二叉树最大宽度
