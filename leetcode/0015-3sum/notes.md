# 15. 3Sum

## 思路

先排序，固定第一个数 `nums[i]`，再用左右双指针在后半段寻找和为 `-nums[i]` 的两个数。

为了避免重复答案：

- 固定点 `i` 遇到相同值时跳过。
- 找到一个三元组后，左右指针都跳过相同值。

如果固定点已经大于 `0`，后面的数也都不小于它，不可能再凑出 `0`，可以提前结束。

## 复杂度

- 时间复杂度：`O(n^2)`
- 空间复杂度：`O(1)`，不计排序和答案数组
