# 84. Largest Rectangle in Histogram

## 思路

用单调递增栈保存柱子的起点和高度。遇到更矮的柱子时，说明栈顶高度能延伸到的右边界已经确定，于是不断弹栈并计算面积。

弹出高柱子后，要把它最早能延伸到的位置留给当前矮柱子，这样当前矮柱子之后也能正确向左扩展。最后追加一个高度 `0` 的哨兵，把栈中剩余柱子全部结算。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(n)`
