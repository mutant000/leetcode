# Best Time to Buy and Sell Stock IV

## 思路

用动态规划维护每次交易后的买入和卖出状态。

- `buy[t]`：完成第 `t` 次买入后，手上的最大收益。
- `sell[t]`：完成第 `t` 次卖出后，手上的最大收益。

遍历每个价格时，依次更新第 1 次到第 `k` 次交易。

如果 `k` 很大，相当于可以无限次交易，直接把所有上涨差价加起来即可。

## 复杂度

- 时间复杂度：`O(n * k)`
- 空间复杂度：`O(k)`
