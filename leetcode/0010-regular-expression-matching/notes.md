# Regular Expression Matching

## 思路

用动态规划判断 `s` 的前 `i` 个字符能不能被 `p` 的前 `j` 个字符匹配。

- 普通字符和 `.`：如果当前能匹配，就看前一格 `dp[i - 1][j - 1]`。
- `*`：有两种选择：
  - 匹配 0 次，直接丢掉前一个字符和 `*`，看 `dp[i][j - 2]`。
  - 匹配 1 次或更多，如果 `*` 前面的字符能匹配当前 `s[i - 1]`，看 `dp[i - 1][j]`。

## 复杂度

- 时间复杂度：`O(m * n)`
- 空间复杂度：`O(m * n)`
