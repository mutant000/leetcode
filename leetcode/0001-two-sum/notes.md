# 1. Two Sum

## 思路

用哈希表记录“之前见过的数字 -> 它的位置”。

遍历到当前数字 `value` 时，只需要看 `target - value` 是否已经出现过：

1. 如果出现过，说明这两个数相加正好等于 `target`，直接返回两个下标。
2. 如果没出现，把当前数字和下标存进哈希表，留给后面的数字匹配。

## 复杂度

- Time: `O(n)`
- Space: `O(n)`

## 边界

- 两个相同数字也可以配对，但必须是不同下标。
- 题目保证只有一个答案。
