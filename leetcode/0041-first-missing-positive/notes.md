# 41. First Missing Positive

## 思路

长度为 `n` 的数组里，答案只可能在 `1` 到 `n + 1` 之间。把数字 `x` 尽量放到下标 `x - 1` 的位置。

交换完成后，从左到右找第一个 `nums[i] != i + 1` 的位置，缺失的最小正数就是 `i + 1`；如果所有位置都正确，答案就是 `n + 1`。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
