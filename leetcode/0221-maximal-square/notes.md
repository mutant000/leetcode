# Maximal Square

## 思路

用动态规划记录以当前位置为右下角的最大正方形边长。

如果当前位置是 `"1"`，它能形成的正方形边长取决于三个方向：

- 上方
- 左方
- 左上方

这三个位置的最小边长加 1，就是当前位置的最大边长。

用一维数组保存上一行状态，再用 `previous_diagonal` 保存左上方的旧值。

## 复杂度

- 时间复杂度：`O(m * n)`
- 空间复杂度：`O(n)`
