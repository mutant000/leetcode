# 394. Decode String

## 思路

用栈保存进入方括号前的字符串和重复次数。遇到数字累积次数；遇到 `[` 把当前状态入栈并开始新片段；遇到 `]` 时弹出上一层，把当前片段重复后接回去。

`current` 用列表累积字符，最后再 `join`。

## 复杂度

- Time: `O(output length)`
- Space: `O(output length)`

## 边界

- 多位重复次数。
- 嵌套编码。
- 普通字符与编码片段混合。
