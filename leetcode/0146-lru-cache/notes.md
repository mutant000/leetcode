# 146. LRU Cache

## 思路

哈希表负责 `O(1)` 找到节点，双向链表负责维护最近使用顺序。链表左侧靠近虚拟头的是最久未使用，右侧靠近虚拟尾的是最近使用。

每次 `get` 或更新已有 key，都把节点移动到右侧。插入新节点后如果超过容量，就删除左侧第一个真实节点。

## 复杂度

- Time: `O(1)` per operation
- Space: `O(capacity)`

## 边界

- 更新已有 key 不应增加容量。
- 访问过的 key 会变成最近使用。
- 容量满后淘汰最久未使用的 key。
