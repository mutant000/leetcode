# 4. Median of Two Sorted Arrays

## 思路

在较短数组上二分切分位置，让两个数组左半部分总长度等于右半部分，或比右半部分多一个。

合法切分需要满足：

- `left1 <= right2`
- `left2 <= right1`

这样左半部分所有数都不大于右半部分所有数。总长度为奇数时，中位数是左半部分最大值；偶数时，是左半最大值和右半最小值的平均数。

## 复杂度

- 时间复杂度：`O(log(min(m, n)))`
- 空间复杂度：`O(1)`
