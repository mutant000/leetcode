# 447. Number of Boomerangs

## 思路

依次把每个点作为中心点，计算其他点到它的距离平方，并统计每种距离出现的次数。

如果同一距离有 `count` 个点，可以有序选择第二个点和第三个点，共有：

`count * (count - 1)`

种回旋镖。使用距离平方即可比较距离，不需要开平方。

## 复杂度

- 时间复杂度：`O(n^2)`
- 空间复杂度：`O(n)`
