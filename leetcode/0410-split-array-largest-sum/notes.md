# 410. Split Array Largest Sum

## 思路

把“每个子数组和不能超过多少”作为二分答案。下界是数组最大元素，上界是数组总和。

给定一个上限后，从左到右贪心地把尽量多的元素放进当前子数组；一旦加入下一个元素会超过上限，就开始新子数组。这样得到的是该上限下所需的最少子数组数量。如果数量不超过 `k`，说明上限可行，可以继续缩小。

## 复杂度

- 时间复杂度：`O(n log(sum(nums)))`
- 空间复杂度：`O(1)`
