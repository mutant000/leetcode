# 172. Factorial Trailing Zeroes

## 思路

阶乘末尾的 `0` 来自因子 `10 = 2 * 5`。在阶乘中因子 `2` 的数量远多于因子 `5`，所以只需要统计因子 `5` 的数量。

`n // 5` 统计至少贡献一个 `5` 的数，`n // 25` 统计额外贡献第二个 `5` 的数，以此类推。

## 复杂度

- 时间复杂度：`O(log_5 n)`
- 空间复杂度：`O(1)`
