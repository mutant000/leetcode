# 421. Maximum XOR of Two Numbers in an Array

## 思路

从最高位到最低位贪心构造答案。每一轮只保留所有数字到当前位为止的前缀，并尝试把答案当前位设为 `1`。

如果存在两个前缀 `a` 和 `b` 满足 `a ^ b = candidate`，等价于集合中同时存在 `a` 和 `candidate ^ a`，说明这个候选答案可行，可以保留当前位的 `1`。

## 复杂度

- 时间复杂度：`O(31n)`，可写作 `O(n)`
- 空间复杂度：`O(n)`
