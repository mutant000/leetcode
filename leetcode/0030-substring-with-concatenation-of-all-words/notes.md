# 30. Substring with Concatenation of All Words

## 思路

所有单词长度相同，所以可以按单词长度分组滑动窗口。不同起点偏移分别扫描，保证每次移动一个完整单词。

窗口中记录每个单词出现次数。如果遇到不需要的单词，就清空窗口；如果某个单词次数超了，就从左边移出单词直到合法。窗口正好包含所有单词时记录左边界。

## 复杂度

- 时间复杂度：`O(n * L)`，`L` 是单词长度，实际每个偏移下左右指针线性移动
- 空间复杂度：`O(k)`，`k` 是不同单词数量
