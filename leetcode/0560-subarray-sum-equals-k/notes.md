# 560. Subarray Sum Equals K

## 思路

设当前前缀和是 `prefix_sum`。如果之前出现过 `prefix_sum - k`，那么这些位置之后到当前位置的子数组和就是 `k`。

遍历时用哈希表统计每个前缀和出现次数，先查询再加入当前前缀和，就能正确处理从下标 `0` 开始的子数组。

## 复杂度

- Time: `O(n)`
- Space: `O(n)`

## 边界

- 数组里有负数，不能用滑动窗口。
- `k` 为 `0`。
- 多个相同前缀和产生多段答案。
