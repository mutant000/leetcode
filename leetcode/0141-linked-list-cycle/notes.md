# 141. Linked List Cycle

## 思路

用快慢指针判断链表是否有环。

1. `slow` 每次走一步。
2. `fast` 每次走两步。
3. 如果链表有环，快指针一定会在环里追上慢指针。
4. 如果没有环，快指针会先走到空。

## 复杂度

- Time: `O(n)`
- Space: `O(1)`

## 边界

- 空链表没有环。
- 只有一个节点且自己指向自己时，有环。
