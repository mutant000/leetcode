# 103. Binary Tree Zigzag Level Order Traversal

## 思路

先按普通层序遍历拿到每一层的节点值，再用一个布尔变量记录当前层的方向。

第一层从左到右，下一层从右到左，如此交替。如果当前层需要从右到左，就把这一层的值反转后加入答案。

## 复杂度

- 时间复杂度：`O(n)`，每个节点访问一次
- 空间复杂度：`O(w)`，`w` 是二叉树最大宽度
