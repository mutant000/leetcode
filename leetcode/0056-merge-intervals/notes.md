# 56. Merge Intervals

## 思路

先按区间起点排序。排序后，当前区间只需要和已经合并结果中的最后一个区间比较。

如果当前起点大于最后一个区间的终点，说明不重叠，直接加入；否则更新最后一个区间的终点为两者终点的较大值。

## 复杂度

- Time: `O(n log n)`
- Space: `O(n)`

## 边界

- 相邻端点相等也算重叠。
- 一个区间完全包含另一个区间。
- 输入顺序无序。
