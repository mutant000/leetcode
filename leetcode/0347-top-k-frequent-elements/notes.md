# 347. Top K Frequent Elements

## 思路

先用哈希表统计频率。频率最大不会超过 `len(nums)`，所以可以建立频率桶：`buckets[f]` 保存出现 `f` 次的数字。

从高频桶向低频桶收集数字，拿到 `k` 个就返回。

## 复杂度

- Time: `O(n)`
- Space: `O(n)`

## 边界

- `k` 等于不同数字个数。
- 多个数字频率相同，返回任意合法顺序。
- 数组长度为 `1`。
