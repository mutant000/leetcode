# 75. Sort Colors

## 思路

使用荷兰国旗三指针：

- `[0, left)` 都是 `0`
- `[left, index)` 都是 `1`
- `(right, end]` 都是 `2`

扫描到 `0` 就换到左边，扫描到 `2` 就换到右边；换来右侧未知元素后，`index` 不能立刻右移，需要继续检查。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
