# 424. Longest Repeating Character Replacement

## 思路

使用滑动窗口。窗口内出现次数最多的字符不需要替换，其余字符都需要替换，所以需要的替换次数是：

`窗口长度 - 窗口内最高字符频率`

如果这个值大于 `k`，就不断移动左边界；否则当前窗口合法，可以更新最长长度。

`max_frequency` 不必在左边界移动时减小。旧的较大值只会让窗口保持在曾经达到过的最佳长度，不会产生更大的错误答案。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`，计数数组固定为 26 个位置
