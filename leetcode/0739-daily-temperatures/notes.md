# 739. Daily Temperatures

## 思路

单调栈保存还没找到更高温度的日期下标，栈内温度从底到顶递减。

遍历到更高温度时，持续弹出栈顶日期，并用当前日期差更新答案。最后栈里剩下的日期没有更高温度，答案保持 `0`。

## 复杂度

- Time: `O(n)`
- Space: `O(n)`

## 边界

- 温度一直下降。
- 温度一直上升。
- 相同温度不算更高。
