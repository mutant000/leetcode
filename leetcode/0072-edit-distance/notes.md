# 72. Edit Distance

## 思路

定义 `dp[i][j]` 为 `word1[:i]` 变成 `word2[:j]` 的最少操作数。

如果当前字符相同，就继承左上角状态；否则可以插入、删除或替换一个字符，对应左、上、左上三个状态中的最小值再加一。

实现时只保留上一行和当前行，将空间从二维表压到一维。

## 复杂度

- Time: `O(m * n)`
- Space: `O(n)`

## 边界

- 任意一边为空时，答案是另一边长度。
- 两个字符串完全相同时答案是 `0`。
- 插入、删除、替换都可能是最优操作，需要三者一起比较。
