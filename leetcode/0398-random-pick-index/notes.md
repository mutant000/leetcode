# 398. Random Pick Index

## 思路

初始化时遍历数组，用哈希表保存每个数字出现过的全部下标。

调用 `pick(target)` 时，直接在 `target` 对应的下标列表中等概率随机选择一个。这样把一次性的预处理放在构造函数里，适合题目中最多进行多次 `pick` 调用的情况。

## 复杂度

- 初始化时间复杂度：`O(n)`
- 每次 `pick` 的时间复杂度：`O(1)`
- 空间复杂度：`O(n)`
