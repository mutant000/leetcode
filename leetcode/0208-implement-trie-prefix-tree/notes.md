# 208. Implement Trie (Prefix Tree)

## 思路

每个节点保存两类信息：

- `children`：当前字符之后能走到哪些子节点。
- `is_word`：从根到当前节点是否刚好组成一个完整单词。

插入时逐字符向下走，没有子节点就新建。查询完整单词时需要路径存在并且末尾节点 `is_word` 为真；查询前缀时只需要路径存在。

## 复杂度

- 时间复杂度：`O(L)`，`L` 是单词或前缀长度
- 空间复杂度：`O(total characters)`，取决于插入的字符总数
