# 230. Kth Smallest Element in a BST

## 思路

二叉搜索树的中序遍历是升序。

用栈做迭代中序遍历，每弹出一个节点就说明访问到了当前第几个小的值：
- 弹出节点后让 `k -= 1`。
- 当 `k == 0` 时，当前节点值就是第 `k` 小。

因为只需要第 `k` 个值，找到后可以直接返回。

## 复杂度

- Time: `O(h + k)`
- Space: `O(h)`

## 边界

- `k = 1`。
- `k` 等于节点总数。
- 树完全偏左或偏右。
