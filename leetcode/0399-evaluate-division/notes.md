# 399. Evaluate Division

## 思路

把每个变量看成图里的点。等式 `a / b = value` 就是两条有权边：

- `a -> b` 的权重是 `value`
- `b -> a` 的权重是 `1 / value`

查询 `x / y` 时，从 `x` 出发 BFS 找到 `y`，路径上的权重乘积就是答案。找不到路径或者变量不存在，就返回 `-1.0`。

## 复杂度

- 建图时间复杂度：`O(E)`
- 每个查询时间复杂度：`O(V + E)`
- 空间复杂度：`O(V + E)`
