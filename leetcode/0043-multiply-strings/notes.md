# Multiply Strings

## 思路

模拟竖式乘法。

`num1[i] * num2[j]` 的结果会落在结果数组的 `i + j` 和 `i + j + 1` 两个位置：

- `i + j + 1` 放当前位。
- `i + j` 放进位。

最后跳过结果开头的 0，把数字数组拼成字符串。

## 复杂度

- 时间复杂度：`O(m * n)`
- 空间复杂度：`O(m + n)`
