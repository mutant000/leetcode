# Interleaving String

## 思路

用动态规划判断 `s3` 的前缀能不能由 `s1` 和 `s2` 的前缀交错组成。

`dp[j]` 表示当前用了 `s1` 的前 `i` 个字符、`s2` 的前 `j` 个字符时，能不能组成 `s3` 的前 `i + j` 个字符。

每一格有两个来源：

- 从 `s1` 拿当前字符。
- 从 `s2` 拿当前字符。

只要其中一种来源成立，这个状态就成立。

## 复杂度

- 时间复杂度：`O(len(s1) * len(s2))`
- 空间复杂度：`O(len(s2))`
