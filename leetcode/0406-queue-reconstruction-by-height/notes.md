# 406. Queue Reconstruction by Height

## 思路

先把人按身高从高到低排序；身高相同时，按 `k` 从小到大排序。

随后依次把每个人插入队列的下标 `k`。处理当前人时，队列中已有的人都不比他矮，因此插入位置前面恰好有 `k` 个身高大于或等于他的人。之后插入的矮个子不会影响这个计数。

## 复杂度

- 时间复杂度：`O(n^2)`，列表中间插入需要移动元素
- 空间复杂度：`O(n)`
