# 173. Binary Search Tree Iterator

## 思路

二叉搜索树的中序遍历是升序。迭代器只需要按需模拟中序遍历。

初始化时把根节点一路向左的节点压栈。`next` 弹出栈顶作为当前最小值，然后把它的右子树一路向左压栈。`hasNext` 判断栈是否非空。

## 复杂度

- `next` / `hasNext` 平均时间复杂度：`O(1)`
- 空间复杂度：`O(h)`，`h` 是树高
