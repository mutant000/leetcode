# 918. Maximum Sum Circular Subarray

## 思路

最大环形子数组有两种情况：

1. 不跨过数组首尾，就是普通最大子数组和。
2. 跨过首尾，相当于总和减去中间一段最小子数组和。

用 Kadane 算法同时求最大子数组和、最小子数组和以及数组总和。若所有数都是负数，`total - min_sum` 会变成空数组，此时直接返回最大子数组和。

## 复杂度

- 时间复杂度：`O(n)`
- 空间复杂度：`O(1)`
