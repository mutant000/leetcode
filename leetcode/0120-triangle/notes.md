# 120. Triangle

## 思路

从三角形底部向上做动态规划。`dp[col]` 表示从当前处理到的位置往下走的最小路径和。

初始化为最后一行。每向上一行，就用下面相邻两个位置的较小值更新当前格子：`triangle[row][col] + min(dp[col], dp[col + 1])`。

最后 `dp[0]` 就是从顶部到底部的最小路径和。

## 复杂度

- 时间复杂度：`O(n^2)`
- 空间复杂度：`O(n)`，`n` 是三角形行数
