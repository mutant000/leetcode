# 62. Unique Paths

## 思路

机器人只能向右或向下走，到达某个格子的路径数等于上方格子路径数加左侧格子路径数。

用一维 `dp` 表示当前行每一列的路径数。初始化全为 `1`，表示第一行只能一直向右。逐行更新时，`dp[column]` 原本是上方路径数，`dp[column - 1]` 是左侧路径数，相加即可。

## 复杂度

- Time: `O(m * n)`
- Space: `O(n)`

## 边界

- 只有一行或一列时答案都是 `1`。
- `m` 和 `n` 最大时结果仍在题目保证范围内。
