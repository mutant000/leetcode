# Largest Number

## 思路

关键是决定两个数字字符串 `a` 和 `b` 的顺序。

如果 `a + b` 比 `b + a` 更大，就应该把 `a` 放前面；否则把 `b` 放前面。

排序后拼接全部字符串即可。最后如果开头是 `0`，说明所有数字都是 0，直接返回 `"0"`。

## 复杂度

- 时间复杂度：`O(n log n * k)`，`k` 是数字字符串长度
- 空间复杂度：`O(n * k)`
